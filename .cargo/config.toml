[alias]
xtask = "run --package xtask --release --"
git = "xtask git"
cargo = "xtask cargo"
tiny = "xtask tiny"

[net]
retry = 2
offline = true

[http]
proxy = "192.168.111.1:10808"

[term]
quiet=true
verbose = false

# [build]
# target = "riscv64gc-unknown-none-elf"
