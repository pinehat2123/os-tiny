[workspace]
members = [
    "kernel", 
    "linker", 
    "xtask", 
    "easy-fs", 
    "allocator", 
    "context", 
    "vm", 
    "signal-defs", "signal-impl", "signal"
    ]
default-members = ["xtask"]

exclude = ["mod_mgmt"]


[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"